import { render, screen } from "@testing-library/react";
import { MainHeader } from "../MainHeader";

describe("test main header", () => {
  test("test title and subtitle", () => {
    render(<MainHeader />);
    //const title = screen.getBy;
  });
});
